import { Container } from "@chakra-ui/react";
import { NotesList } from "../components/notes-list";

export const Notes = () => {
  return (
    <Container>
      <NotesList />
    </Container>
  );
};
